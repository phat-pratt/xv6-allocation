// struct frame {
//     int frameNumber; // frame number(physical address of frame)
//     int pid; // pid of the allocated process
//     //struct frame *next; // pointer to next frame
//     struct frame *prev; // pointer to next frame
// };

int*    getframes(void);
